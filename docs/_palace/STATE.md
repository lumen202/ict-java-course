# STATE — `docs/` (2026-08-07)

Protocol: [`../MIND_PALACE.md`](../MIND_PALACE.md)

- `HANDOFF.md` — the single resume point: status, next steps, decisions.
  Rewritten every session; history is preserved by `logs/`, not by HANDOFF.
- `CODEBASE_MAP.md` — the index: file-by-file tour, request flows, palace
  pointers.
- `MIND_PALACE.md` — the memory protocol (per-folder `_palace/` + logs).
- `logs/` — project-wide session logs, one file per session, append-only.
- Folder `README.md`s in `src/app/`, `src/components/`, `src/lib/content/`,
  `supabase/` — timeless how-it-works docs. Root `README.md` — human setup +
  deploy guide.
